# Core dependencies for Locus PDF Search
PyMuPDF>=1.23.0          # PDF text extraction (imported as fitz)
rank-bm25>=0.2.2         # BM25 keyword retrieval
fastembed>=0.2.0         # Lightweight ONNX-based embeddings (replaces sentence-transformers)
numpy>=1.24.0
customtkinter>=5.2.0     # Modern GUI